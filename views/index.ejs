<%- include('partials/head.ejs') %>
<body id="bodyHome">
    <%- include('partials/header.ejs') %>
    <main>
        <div id="gameContainer">
            <div>
                <p id="numeral">#:<span id="rollNumber"><%= user ? parseInt(user.roll) : 5000 %></span></p>
            </div>
            <div id="btnHolder">
                <% if (user) { %>
                    <form action="/roll" method="POST">
                        <input type="hidden" name="user" value="<%= user ? JSON.stringify(user) : null %>">
                        <input type="hidden" name="whatToDo" value="higher">
                        <button id="btnHi">HIGHER</button>
                    </form>
                <% } else { %>
                    <a href="/login"><button id="btnHi">HIGHER</button></a>
                <% } %>
                <p>-</p>
                <% if(user){ %>
                    <form action="/roll" method="POST">
                        <input type="hidden" name="user" value="<%= user ? JSON.stringify(user) : null %>">
                        <input type="hidden" name="whatToDo" value="lower">
                        <button id="btnLo">LOWER</button>
                    </form>
                <% }else{ %>
                        <a href="/login"><button id="btnLo">LOWER</button></a>
                <% } %>
            </div>
            <div>
                <p id="score">SCORE:<span><%= user ? user.score : 1000 %></span></p>
                <% if (error) { %>
                    <p><%= error %></p>
                <% } %>
            </div>
        </div>
<%- include('partials/footer.ejs') %>